#include <stdio.h>
int main(void)
{
  int n,i,prime=0;
  printf ("Enter any number:");
  scanf ("%d", &n);

  for (i==1; i<=n; i++);
  {
    prime++;
  }

  if (prime==2)
  printf ("Prime Number");

  else
  printf("Composite Number");

  return 0;
}
